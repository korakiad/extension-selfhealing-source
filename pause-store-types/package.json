{
  "name": "@qa-debug/pause-store-types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared PauseStore interface + payload types + toFailureContextView helper. Consumed by qa-debug-mcp (InMemoryPauseStore stub) and extension (MementoPauseStore).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:check": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
