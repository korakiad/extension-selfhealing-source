{
  "name": "qa-debug-companion",
  "version": "0.0.0",
  "private": true,
  "description": "VS Code extension + dynamic MCP registration that holds Mocha on test failure for Copilot-driven QA investigation. Phase 1.",
  "engines": {
    "node": ">=18"
  },
  "packageManager": "pnpm@10.7.0",
  "scripts": {
    "build": "pnpm -r --filter ./mocha-hooks --filter ./qa-debug-mcp --filter ./extension run build",
    "build:hooks": "pnpm --filter ./mocha-hooks run build",
    "build:mcp": "pnpm --filter ./qa-debug-mcp run build",
    "build:extension": "pnpm --filter ./extension run build",
    "test:s2": "pnpm --filter ./mocha-hooks run build && node --import tsx tools/oracle.ts --decisions retry,mark_passed,give_up --tests 'fixture-tests/specs/**/*.spec.js'",
    "clean": "pnpm -r exec rm -rf dist"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.2"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
