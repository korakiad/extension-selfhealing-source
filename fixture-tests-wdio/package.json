{
  "name": "@qa-debug/fixture-tests-wdio",
  "version": "0.0.0",
  "private": true,
  "description": "Fixture suite for verifying v5.2 Mode A (transparent webdriverio.remote() integration). Mirrors fixture-tests/ structure but uses mocha + wdio v8 standalone instead of bare mocha.",
  "scripts": {
    "test": "mocha"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^22.10.0",
    "mocha": "^10.7.3",
    "webdriverio": "^8.40.6"
  }
}
